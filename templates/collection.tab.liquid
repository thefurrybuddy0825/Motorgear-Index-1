{% layout none %}
<div class="products-grid">
  <div class="grid-item">
    {% for product in collection.products %}
    	<div class="block">
    	  <div>
    	    <div class="item sp-item">
    	      {% render 'product-grid-item-style-1', product: product, sectionId: collection.title, append: forloop.index %}
    	    </div>
    	  </div>
    	</div>
    {% endfor %}
  </div>
</div>{%- comment -%}

This file has been rewritten to preserve the original behavior.

---
Changes:
- from: "{% render 'product-grid-item-style-1', product: product, sectionId: collection.title
    | handle | append: forloop.index %}"
  to: "{% render 'product-grid-item-style-1', product: product, sectionId: collection.title,
    append: forloop.index %}"
  position: 202..312
{%- endcomment -%}